export interface ProfilePayload {
  avatar_file: string;
  bio: string;
  first_name: string;
  last_name: string;
}

export interface ProfileResponse {
  avatar_file: string;
  bio: string;
  first_name: string;
  last_name: string;
}
