import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    flexDirection: "row",
    gap: 12,
    marginBottom: 16,
  },
  inputBox: {
    gap: 16,
    marginBottom: 16
  },
  createDecksButton: {
    alignSelf: "center",
    maxWidth: 373,
  },
  infoBox: {
    alignItems: "center",
  },
});
