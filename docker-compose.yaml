version: '3.8'

services:
  nginx-frontend:
    build: .
    container_name: frontend
    ports:
      - "80:80"
    networks:
      - app-network # Внутреннее имя для этого файла

networks:
  app-network:
    driver: bridge