import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  buttonContainer: {
    position: 'absolute',
    width: '100%',
    bottom: 30,
    paddingHorizontal: 10,
    alignItems: 'center',
  },
});
